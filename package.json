{
  "name": "mini-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -k \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
