# Copy to .env and set your real key
GEMINI_API_KEY="your_api_key"
# Optional: override model (keep as gemini-1.5-flash or change as needed)
GEMINI_MODEL=gemini-1.5-flash
# Optional: API base (use only if you need to override)
# GEMINI_API_BASE=https://generativelanguage.googleapis.com/v1beta2
PORT=4000
